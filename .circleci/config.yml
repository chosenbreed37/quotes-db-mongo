version: 2
jobs:
  deply-job:
    docker:
      - image: quotes-db-mongo
    working_directory: /tmp/quotes-db-mongo
    steps:
      - run:
          name: Spin up db
          command: ./run.sh

workflows:
  version: 2
    build-deploy:
      jobs:
        - deploy-job
                  