version: 2
jobs:
  deploy-job:
    docker:
      - image: quotes-db-mongo
    working_directory: /tmp/quotes-db-mongo
    steps:
      - run:
          name: Spin up db
          command: ./run.sh

workflows:
  version: 2
    deploy:
      jobs:
        - deploy-job
                  